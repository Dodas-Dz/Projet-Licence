const projectname= document.getElementById(projectname);
const form = document.getElementById(formulairevalid);

form.addEventListener('submit' , (e)=> {
let message = []
if (projectname.value == )


})